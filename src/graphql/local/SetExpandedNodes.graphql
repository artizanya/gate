# Hey Emacs, this is -*- coding: utf-8 -*-

mutation SetExpandedNodes($path: [String!]!, $expanded: Boolean!) {
  setProcessTreeItem(path: $path, expanded: $expanded) @client {
    path
    expanded
  }
}
